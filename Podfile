# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Project LightBringers' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  # use_frameworks!

#  # Pods for Project LightBringers

  target 'Project LightBringersTests' do
    inherit! :search_paths
    platform :ios, '9.0'
    # Pods for testing
  end

  target 'Project LightBringersUITests' do
    inherit! :search_paths
    platform :ios, '9.0'
    # Pods for testing
  end

end
